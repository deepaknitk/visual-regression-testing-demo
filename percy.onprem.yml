version: 2

snapshot:
  widths: [1440]  # Desktop-only: large desktop
  
  percy-css: |
    /* Hide dynamic elements */
    .loading-spinner { display: none !important; }
    .timestamp { display: none !important; }
    
    /* Disable animations */
    *, *::before, *::after {
      animation-duration: 0s !important;
      transition-duration: 0s !important;
    }
